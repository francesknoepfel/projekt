# wahrscheinlich kann ich dieses file löschen
# import json

# with open('daten/datensatz.json') as open_file:
   # datensatz = json.load(open_file)

# datensatz["priority"] = "High"
# datensatz["deadline"] = "Juni"

# with open("daten/datensatz.json", "w", encoding="utf8") as open_file:
    # json.dump(datensatz, open_file, indent=4)
